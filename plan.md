# Running Log — 작업 기획서

버전: 1.0 · 작성일: 2026-09-08

이 문서는 Claude Code에 전달할 초기 버전 구현 명세다. 기존 논의의 권장안을 기본 정책으로 채택했다. 명시된 범위 안에서 구현하고, 제품 정책을 임의로 바꾸지 않는다. 패키지 버전과 배포 서비스의 지원 범위·제약은 개발 시작 시 공식 문서에서 확인하고 고정한다.

## 1. 서비스 목적과 구현 범위

그룹에서 정한 주간 목표 거리를 각 회원이 달성하고, 증거 사진을 첨부한 러닝 기록을 관리자 승인 후 합산하는 모바일 우선 웹 서비스다. 매주 개인 순위와 성공·실패를 자동 집계하고 지난 기록을 조회한다.

### 필수 범위

- 이메일 회원가입·로그인·로그아웃·비밀번호 재설정
- 필수 닉네임·프로필 사진 등록 및 수정
- 그룹 생성, 초대 링크·코드, 참여 요청·승인·거절·취소
- 회원당 활성 그룹 1개, 그룹당 관리자 1명
- 주간 달력, 개인 진행률, 승인 거리 기준 순위, 과거 주차 조회
- 오늘의 러닝 기록 및 사진 여러 장 등록
- 관리자 기록 승인·반려·승인 취소
- 기록 상세, 사진 확대, 댓글 작성·삭제
- 주간 자동 집계, 요약 카드, 요약 이미지 저장, 자동 표시 확인 상태
- 앱 내 알림
- 관리자 권한 위임, 회원 탈퇴, 빈 그룹 보관
- Next.js / Supabase / Vercel / PWA

### 제외 범위

소셜 로그인, 공개 그룹 검색, 복수 그룹 참여, 복수 관리자, GPS 측정, 운동 서비스 연동, 사진 OCR·조작 자동 판정, 벌금 결제·벌칙 이행 추적, 푸시·문자 알림, 오프라인 작성·전송 대기열, 과거 날짜 소급 등록, 확정 결과 수정, 운영자용 별도 관리자 사이트, 회원 강제 퇴장, 계정 삭제 UI는 초기 버전에서 제외한다.

## 2. 확정 제품 정책

| 항목 | 정책 |
|---|---|
| 목표 | 그룹 전체 공통인 1인당 주간 목표 거리 |
| 주간 경계 | Asia/Seoul 기준 월요일 00:00 이상, 다음 월요일 00:00 미만 |
| 공식 합계 | 해당 주 운동 기록 중 승인 완료 거리만 합산 |
| 성공 | 승인 합계가 그 주 목표 이상 |
| 실패 | 평가 대상자의 승인 합계가 그 주 목표 미만 |
| 동점 | 공동 순위: 1, 2, 2, 4 방식 |
| 하루 기록 | 여러 건 허용 |
| 기록 날짜 | 서버가 판단한 한국 시간 오늘만 등록 가능 |
| 거리 | 소수 둘째 자리까지, 내부 정수 미터 저장 |
| 기록 사진 | 1~5장, 원본 파일당 최대 10MB |
| 자기 승인 | 관리자는 본인 기록도 직접 승인 가능 |
| 주중 참여 | 첫 주는 준비 주간, 다음 주부터 평가 |
| 목표·벌칙 변경 | 다음 주부터 적용 |
| 집계 유예 | 월요일 12:00 미만까지 지난주 기록 검토 가능 |
| 미처리 기록 | 마감 시 승인 기한 만료, 합계 제외 |
| 확정 결과 | 수정 불가 |
| 자동 요약 | 회원·그룹·주차당 최대 한 번 자동 표시 |

거리 입력 상한은 기록당 500km, 주간 목표는 1,000km로 설정하고 상수로 관리한다. 허용 범위를 입력란에 표시한다. 소수점 초과 자릿수는 묵시적으로 반올림하지 않고 오류를 안내한다.

## 3. 회원과 그룹 생명주기

### 3.1 회원가입

1. 이메일·비밀번호로 계정을 생성하고 이메일 인증을 완료한다.
2. 닉네임과 프로필 사진을 등록한다.
3. 프로필 입력 완료 후 그룹 화면에 진입한다.

프로필 미완료 계정은 다음 로그인 시 프로필 완성 화면으로 이동한다. 닉네임은 앞뒤 공백 제거 후 2~20자로 제한하고 중복을 허용한다. 식별과 권한 판단은 닉네임 대신 회원 ID를 사용한다. 프로필 사진은 1장 필수, 최대 10MB다.

초대 링크를 통해 진입한 사용자는 로그인·인증·프로필 등록 후 원래 그룹의 참여 안내로 돌아온다. 돌아갈 경로는 서비스 내부 경로만 허용한다.

### 3.2 그룹 미참여 상태

- 요청 없음: `그룹 만들기`, `초대 코드로 참여하기` 표시
- 참여 요청 중: 그룹명·목표·벌칙, 승인 대기 안내, 요청 취소 표시
- 거절됨: 거절 안내 후 다른 그룹 요청 또는 그룹 생성 가능

한 회원은 활성 멤버십 1개, 대기 참여 요청 1개만 보유한다. 요청 중에는 취소 후 그룹 생성 또는 다른 요청을 할 수 있다. 참여 승인은 처리 시점에 활성 멤버십 유무를 다시 검사한다.

### 3.3 그룹 생성

필수 입력은 그룹명(2~30자), 1인당 주간 목표 거리, 실패 시 벌칙(1~500자)이다. 벌칙이 없는 그룹은 `없음`을 입력할 수 있다. 생성자는 같은 트랜잭션에서 관리자 멤버로 등록된다.

그룹 생성과 함께 예측하기 어려운 초대 코드와 링크를 발급한다. 그룹 생성 주차도 준비 주간 규칙을 적용한다. 생성 전 주차는 조회 목록에 나타나지 않는다.

### 3.4 그룹 설정 및 탈퇴

- 그룹명은 즉시 변경되며 확정 결과의 그룹명 스냅샷에는 영향을 주지 않는다.
- 목표·벌칙의 변경 예약은 다음 주에 적용한다. 같은 주에 다시 수정하면 다음 주 예약을 대체한다.
- 관리자는 현재 활성 멤버에게 권한을 위임할 수 있다. 원자적으로 변경하여 항상 관리자 1명을 유지한다.
- 일반 멤버는 그룹에서 탈퇴할 수 있다. 탈퇴 확인창에서 기록 보존과 접근 권한 종료를 설명한다.
- 관리자는 다른 멤버가 있으면 위임 후 탈퇴해야 한다. 마지막 멤버인 관리자가 탈퇴하면 그룹을 보관 상태로 전환한다.
- 보관 그룹은 새 참여와 기록 작성을 막지만 예정된 주간 마감은 완료한다.
- 탈퇴자는 그룹 기록·사진·댓글을 더 이상 조회하지 못한다. 그룹 내 기존 기록과 결과는 남는다.
- 탈퇴 전에 제출한 대기 기록은 관리자가 계속 검토할 수 있다.
- 주초 평가 대상이었던 멤버는 주중 탈퇴해도 해당 주 평가에서 제외하지 않는다. UI에는 `탈퇴`를 함께 표시한다.
- 재참여에도 관리자 승인이 필요하다. 주중 재참여를 이용해 기존 주간 평가 대상 여부를 초기화할 수 없다.

현재 그룹 멤버는 그 그룹의 과거 기록 전체를 볼 수 있다. 참여 전 기록도 포함한다. 댓글 작성 권한은 현재 멤버에게만 있다.

## 4. 화면 및 탐색 구조

| 경로 | 화면 |
|---|---|
| `/` | 회원 상태별 메인, 주간 대시보드 |
| `/login`, `/signup` | 로그인·회원가입 |
| `/forgot-password`, `/reset-password` | 비밀번호 재설정 |
| `/onboarding` | 필수 프로필 완성 |
| `/join/[code]` | 초대 및 참여 안내 |
| `/groups/new` | 그룹 생성 |
| `/records/[id]` | 기록 상세 |
| `/admin` | 참여 요청·기록 검토·멤버·그룹 설정 |
| `/profile` | 프로필 수정·로그아웃·그룹 탈퇴 |
| `/notifications` | 앱 내 알림 |
| `/offline` | 오프라인 안내 |

주차는 `/?week=YYYY-MM-DD` 형태로 월요일 날짜를 유지한다. 기록 상세에서 돌아오면 원래 주차와 가능한 범위에서 스크롤 위치를 복원한다. 직접 상세 URL로 진입한 경우 기본 메인으로 돌아간다.

### 4.1 메인 화면 배치

1. 그룹명, 알림, 프로필, 관리자 진입 버튼
2. 주간 이동: 이전 주 / 날짜 범위 / 다음 주 / 이번 주
3. 해당 주 목표·벌칙과 나의 승인 거리·남은 거리·대기 거리
4. 멤버별 주간 달력 및 순위
5. 오늘의 기록 등록 영역

상단 `오늘 기록 올리기` 버튼은 등록 영역으로 이동한다. 지난주 조회 상태에서는 이번 주로 전환하며 `오늘 기록은 이번 주에 등록됩니다`를 안내한다. 미래 주차는 조회하지 않는다. 그룹 설정의 다음 주 예약만 별도로 확인할 수 있다.

### 4.2 진행 상황과 달력

- 나의 진행률은 승인 거리 / 목표로 계산한다. 진행 막대는 100%까지 채우고 실제 합계와 초과 달성은 별도로 표시한다.
- 대기 거리는 공식 합계에 더하지 않고 별도 문구로 표시한다.
- 달력은 멤버별 행, 월~일 열, 승인 합계 구조다.
- 같은 날짜에 여러 기록이 있으면 승인 합계와 대기 합계를 구분한다.
- 한 건인 날짜는 상세로 이동하고, 여러 건인 날짜는 목록을 열어 선택한다.
- 반려·기한 만료 기록은 합계에서 제외하되 날짜의 기록 목록에서 상태를 확인할 수 있다.
- 같은 합계는 공동 순위이며 행 표시 순서는 가입 시각, 회원 ID로 고정한다.
- 준비 주간 멤버는 별도 영역에 합계 내림차순으로 표시하고 공식 순위를 부여하지 않는다.
- 진행 중 주차는 `달성 / 도전 중`, 확정 주차는 `성공 / 실패`를 사용한다.
- 기록 없는 평가 대상자도 0km로 표시한다.

모바일에서는 멤버명과 합계를 쉽게 확인하게 하고 날짜 영역은 가로 스크롤을 허용한다. 내 행을 강조한다. 상태를 색상만으로 구분하지 않는다. 과거 주차에서는 당시 평가 대상과 준비 주간 참여자를 기준으로 표시한다.

## 5. 기록 등록·검토·댓글

### 5.1 등록 흐름

입력은 오늘 날짜(읽기 전용), 거리(필수), 증거 사진(필수), 메모(선택, 최대 1,000자)다. 미리보기·사진 삭제·순서 변경을 제공한다.

모바일 사진은 JPEG·PNG·WebP를 지원하고 HEIC/HEIF 입력도 브라우저 또는 서버에서 변환하여 처리한다. 파일 확장자만 신뢰하지 않고 실제 이미지 형식과 용량을 검사한다. 업로드 전 리사이즈·압축을 적용하되 기록 숫자가 읽혀야 한다. 저장 이미지에서 위치 등 EXIF 메타데이터를 제거한다.

증거 사진은 브라우저에서 Supabase Storage로 직접 업로드하고, 서버에서 소유권과 파일 상태를 확인한 후 기록을 최종 생성한다. Vercel 함수가 모든 원본 사진을 한 번에 중계하는 구조는 피한다.

모든 사진 업로드가 완료되어야 기록을 제출한다. 부분 실패 시 성공한 파일을 유지하고 실패 파일만 재시도할 수 있게 한다. 미제출 임시 업로드는 정기 정리한다.

등록 날짜는 최종 제출 시점의 서버 한국 날짜로 결정한다. 자정을 넘기면 자동으로 날짜를 바꿔 제출하지 않고 날짜 변경 안내 후 입력을 다시 확인하게 한다. 어제 기록은 제출할 수 없다.

중복 클릭과 네트워크 재시도는 동일 제출 키로 처리하여 기록이 하나만 생성되게 한다. 성공 후 대기 기록을 즉시 화면에 표시한다.

### 5.2 기록 상태

| 상태 | 의미 및 전이 |
|---|---|
| `pending` | 제출·재제출 완료 → 승인 또는 반려 |
| `approved` | 공식 합계 반영 → 확정 전 관리자 승인 취소 시 pending |
| `rejected` | 반려 사유 필수 → 작성자 수정·재제출 시 pending |
| `expired` | 주간 검토 기한 종료, 합계 제외, 수정 불가 |

작성자는 한국 시간 당일이며 해당 주가 열려 있는 경우에만 pending/rejected 기록을 수정·삭제·재제출할 수 있다. 수정된 pending 기록은 pending을 유지하며 검토 전 내용을 다시 확인하게 한다. 날짜가 지난 후와 월요일 유예 시간에는 회원의 수정·재제출을 허용하지 않는다. 관리자는 월요일 유예 시간에 기존 제출 내용만 검토한다.

승인된 기록은 작성자가 수정·삭제하지 못한다. 관리자는 최종 확정 전 승인 취소를 할 수 있으며 사유를 남긴다. 승인 취소는 공식 합계와 순위에 즉시 반영한다. 확정 이후에는 관리자도 기록 상태·거리·사진을 변경하지 못한다.

관리자의 자기 승인을 허용한다. 승인자·시각·사유와 상태 변경 이력을 남긴다. 관리자 외 사용자는 승인할 수 없다.

### 5.3 상세와 댓글

상세에는 프로필, 닉네임, 운동 날짜, 거리, 메모, 상태, 사진 전체, 반려·승인 취소·기한 만료 안내, 댓글, 메인 이동 링크를 표시한다. 사진 확대 뷰를 제공한다.

현재 같은 그룹의 멤버는 대기·반려를 포함한 기록을 조회하고 댓글을 작성할 수 있다. 댓글은 일반 텍스트 1~1,000자, 오래된 순서이며 본인 또는 관리자가 삭제할 수 있다. 댓글 수정과 대댓글은 제외한다. 주간 확정 후에도 댓글 작성·삭제는 가능하다. 삭제는 화면에서 내용을 제거하고 권한 검증에 필요한 감사 정보만 남긴다.

## 6. 관리자 및 알림

관리자 화면은 `참여 요청`, `기록 검토`, `멤버`, `그룹 설정`으로 구성한다. 메인에 요청·대기 기록 수를 표시한다. 기록 검토는 오래된 순으로 제공하고 사진을 직접 확인한 뒤 개별 승인·반려한다. 일괄 승인은 초기 범위에서 제외한다.

앱 내 알림 대상은 다음과 같다.

- 관리자: 참여 요청, 새 기록 제출, 지난주 검토 미완료 안내
- 회원: 참여 승인·거절, 기록 승인·반려·승인 취소·기한 만료
- 해당 주 참여자: 잠정 결과가 최종 확정되었을 때

알림에는 읽음 상태와 관련 화면 링크를 둔다. 같은 이벤트로 중복 생성하지 않는다. 그룹 탈퇴 후에는 관련 알림에서 기록 내용에 접근할 수 없다. 마감 안내는 월요일 00:00과 11:00 기준 각 1회만 생성하며 실제 예약 작업 실행 시 지연될 수 있다. 앱 내 알림은 비접속 회원에게 푸시로 전송되지 않는다.

## 7. 주간 집계와 요약

### 7.1 평가 대상 확정

주간 시작 시점에 활성 멤버였던 회원이 평가 대상이다. 그 이후 참여자는 준비 주간으로 기록을 등록할 수 있으나 성공·실패·벌칙과 공식 순위에서 제외한다. 다음 주 시작 때 활성 멤버이면 평가 대상이 된다.

평가 대상 여부는 주차별 저장값으로 유지한다. 멤버십 참여·탈퇴 시각을 보존하여 예약 작업이 늦어져도 주간 시작 시점 기준으로 복구할 수 있어야 한다. 같은 회원이 주중 탈퇴·재참여해도 해당 주 결과는 회원당 하나다.

### 7.2 월요일 전환

1. 한국 시간 월요일 00:00을 기준으로 메인은 새 주차를 표시한다.
2. 종료된 주차는 신규 등록을 막는다.
3. 대기 기록이 없으면 종료 주차를 즉시 확정한다.
4. 대기 기록이 있으면 `집계 중`으로 전환하고 월요일 12:00 미만까지 관리자 검토를 허용한다.
5. 대기 기록이 모두 처리되면 12:00 이전이어도 즉시 확정한다.
6. 12:00 이상이면 남은 pending 기록을 expired로 변경한 뒤 확정한다. rejected 상태는 그대로 남는다.

일요일 기록이 월요일에 승인되어도 일요일이 속한 주차에 반영한다. 주차는 승인 시각이 아닌 운동 날짜로 정한다. 승인 요청이 12:00 경계에 걸리면 DB의 단일 트랜잭션에서 현재 시각과 주차 잠금을 기준으로 허용 여부를 결정한다.

집계 중에는 승인 거리 기준 잠정 순위를 표시한다. 개인에게 미처리 기록이 남아 있으면 결과를 `판정 대기`로 표시한다. 전체 결과가 확정되기 전에는 최종 결과처럼 보이지 않도록 카드에 `잠정 결과`를 명시한다.

### 7.3 최종 결과

확정 시 개인 합계·순위·성공 또는 실패·평가 대상 여부와 당시 목표·벌칙·그룹명·표시 닉네임을 저장한다. 그룹 전체 거리는 준비 주간 멤버까지 포함한 승인 거리 합계이며, 성공률의 분모는 평가 대상자만 사용한다. 평가 대상자가 0명이면 성공률은 0% 대신 `평가 대상 없음`을 표시한다.

과거 달력과 결과는 확정된 숫자를 기준으로 표시한다. 프로필 사진은 현재 사진을 보여줄 수 있지만 닉네임·목표·순위·합계 등 결과 텍스트는 확정 스냅샷을 따른다.

### 7.4 자동 요약 정책

자동 표시는 **주간 전환 후 앱을 다음으로 활성 이용할 때** 수행한다. 이미 앱을 열어둔 사용자는 날짜 변경 또는 화면 복귀 시 확인한다. 비접속 상태에서 강제로 화면을 띄우지 않는다.

- 직전 종료 주차에 참여 이력이 있고 현재 해당 그룹의 활성 멤버인 사용자에게 표시한다.
- 여러 주 만에 접속하면 직전 종료 주차만 자동 표시하고 과거 팝업을 연속 재생하지 않는다.
- 미확인 주차가 집계 중이면 잠정 카드, 확정 상태이면 확정 카드를 표시한다.
- 잠정 카드를 이미 자동 표시했다면 확정 때 자동 팝업을 다시 띄우지 않고 앱 내 알림만 제공한다.
- 과거 기록의 `주간 요약 보기`는 확인 상태와 관계없이 계속 사용할 수 있다.
- 서버에 회원·그룹·주차별 확인 레코드를 저장한다. 자동 표시 직전 원자적 claim으로 중복 기기의 동시 표시를 막는다.
- 정확히 한 번의 실제 시청 여부는 보장할 수 없으므로 최대 한 번의 자동 표시를 보장한다. claim 직후 앱이 종료되어 카드를 못 본 경우도 수동 조회로 복구한다.

요약에는 그룹명, 기간, 잠정·확정 표시, 주간 목표, 본인 결과 강조, 개인 순위·거리, 성공자·실패자, 준비 주간 멤버, 그룹 합계, 성공 인원, 벌칙을 포함한다. 모바일에서 읽을 수 있는 HTML 카드로 제공하고 같은 내용을 PNG로 저장한다. 멤버가 많으면 목록을 페이지로 나누어 모든 회원이 이미지에 포함되게 한다. 잠정 이미지에는 잠정 표시를 유지한다.

## 8. 기술 구조

| 영역 | 선택 |
|---|---|
| 프레임워크 | Next.js App Router |
| 언어 | TypeScript, strict 모드 |
| UI | Tailwind CSS, 접근성 있는 공통 컴포넌트 |
| 배포 | Vercel |
| DB | Supabase PostgreSQL |
| 인증 | Supabase Auth, 서버 쿠키 기반 세션 연동 |
| 이미지 | Supabase Storage의 비공개 버킷 |
| 주간 작업 | Vercel Cron이 인증된 서버 엔드포인트 호출 |
| 설치 지원 | PWA 매니페스트, 서비스 워커, 오프라인 안내 |

Server Components를 기본으로 하고 파일 선택, 댓글 입력, 날짜 전환, 모달 등 상호작용 영역만 Client Components로 구성한다. 변경 작업은 Server Actions 또는 Route Handlers에서 인증·권한·입력을 검사한다. 데이터 접근과 도메인 로직을 UI에서 분리한다.

현재 주차 화면은 서버 시각 기준으로 계산하며 예약 작업 성공 여부에 의존하지 않는다. 주차 생성·마감은 재실행 가능한 DB 작업으로 구현하고, 필요한 경우 조회 시에도 누락 주차를 복구한다. Supabase Realtime은 초기 범위에서 제외하며 작업 성공 후 관련 데이터를 갱신하고 화면 복귀 시 재조회한다.

거리 합계는 정수 미터를 사용하고 시간은 UTC 시각으로 저장한다. 주간 키와 운동 날짜 계산에는 Asia/Seoul을 명시한다. 브라우저 시간이나 부동소수점 합계를 판정 근거로 사용하지 않는다.

## 9. 데이터 모델과 무결성

아래는 논리 모델이며 실제 SQL은 마이그레이션으로 작성한다.

| 테이블 | 주요 필드 및 목적 |
|---|---|
| profiles | auth user ID, nickname, avatar_path, onboarding_completed_at |
| groups | name, archived_at, created_at |
| group_invites | group_id, code_hash, created_at; 초대 검증 |
| group_settings | group_id, effective_week_start, target_meters, penalty; 주차별 유효 설정 |
| memberships | group_id, user_id, role, joined_at, left_at; 재참여 이력 보존 |
| join_requests | group_id, user_id, status, reviewed_by, reviewed_at |
| group_weeks | group_id, week_start, target_meters, penalty, state, finalized_at |
| week_members | week_id, user_id, eligible, joined_this_week; 평가 대상 스냅샷 |
| running_records | group_id, user_id, week_id, activity_date, distance_meters, memo, status, version, submission_key |
| record_photos | record_id, storage_path, order_index |
| record_reviews | record_id, actor_id, from_status, to_status, reason, created_at |
| comments | record_id, user_id, body, deleted_at, deleted_by |
| weekly_results | week_id, user_id, total_meters, rank, outcome, nickname_snapshot |
| weekly_summary_views | week_id, user_id, claimed_at |
| notifications | user_id, group_id, event_key, type, target_id, read_at |

group_weeks에는 확정 당시 그룹명과 그룹 합계도 저장한다. 사진 업로드 준비 상태·소유자·만료 시각을 추적하는 임시 업로드 테이블을 별도로 둔다.

필수 제약:

- memberships: left_at이 null인 행은 user_id별 최대 하나
- 활성 그룹은 활성 관리자 한 명을 유지; 생성·위임·탈퇴를 잠금과 트랜잭션으로 처리
- join_requests: pending은 user_id별 최대 하나
- group_weeks: group_id + week_start 유일
- week_members / weekly_results / weekly_summary_views: week_id + user_id 유일
- running_records: user_id + submission_key 유일, 거리 범위 CHECK
- notifications: user_id + event_key 유일
- 날짜·주차·회원·그룹 일치 여부를 서버와 DB 변경 함수에서 검증
- 승인과 제출 수정이 동시에 발생하면 version 검사로 오래된 사진·거리의 승인을 차단
- 상태 변경 및 관련 합계·결과 갱신은 하나의 트랜잭션으로 처리

## 10. 권한과 파일 접근

인증된 사용자가 직접 API를 호출해도 규칙이 유지되도록 RLS와 DB 함수 권한을 설정한다.

- 사용자는 본인의 프로필만 수정할 수 있다. 다른 프로필 조회는 현재 같은 그룹 또는 관리자 검토 대상 참여 요청으로 제한한다.
- 현재 활성 멤버만 해당 그룹의 기록·주차·댓글을 조회한다.
- 초대 수신자는 코드 검증 후 그룹명·목표·벌칙만 확인한다. 멤버 명단과 기록은 참여 승인 전 공개하지 않는다.
- 기록 작성자만 허용 상태·기간에서 수정·삭제한다.
- 현재 관리자만 참여 검토·기록 검토·설정 변경·권한 위임을 수행한다.
- 결과·평가 대상·집계 상태·다른 회원 알림은 클라이언트가 임의로 쓰지 못한다.
- 사진은 비공개 버킷에 저장하고, 권한 확인 후 짧은 만료 시간의 서명 URL로 제공한다. 회원 탈퇴 후에도 이미 발급한 URL은 만료까지 접근 가능하므로 만료 시간을 짧게 설정한다.
- 프로필 사진도 비공개로 두고 본인·같은 그룹·가입 요청 검토 관리자만 접근한다.
- 서비스 역할 키는 서버 전용이다. 노출 가능한 공개 키와 구분한다.
- 비인증 사진·타 그룹 사진을 기록에 연결하지 못하도록 최종 제출 시 객체 소유권을 확인한다.
- 초대 코드 추측과 댓글·기록 요청 남용을 막기 위해 관련 엔드포인트에 요청 제한을 둔다.

## 11. PWA와 모바일 UX

### 매니페스트

- name: Running Log
- short_name: Running Log
- id / start_url / scope: `/`
- display: standalone
- lang: ko-KR
- theme_color / background_color: 실제 UI와 일치하는 색상
- 일반 앱 아이콘 192×192, 512×512
- 안전 영역을 고려한 별도 maskable 아이콘
- iOS Apple Touch Icon

지원 브라우저에서는 설치 버튼을 표시하고, 자동 설치 UI를 지원하지 않으면 해당 환경에 맞는 홈 화면 추가 방법을 안내한다. 설치된 환경에서는 설치 안내를 숨기고 사용자가 닫은 안내도 계속 반복하지 않는다.

### 서비스 워커

- 정적 리소스와 개인정보가 없는 오프라인 안내만 캐시한다.
- 인증 응답, 개인화 HTML, RSC 응답, 그룹 API, 증거·프로필 사진은 캐시하지 않는다.
- 네트워크 연결이 없으면 오프라인 안내와 재시도 버튼을 제공한다.
- 기록·댓글·승인은 온라인 전용이며 오프라인 요청을 성공처럼 표시하지 않는다.
- 업데이트 시 버전별 정적 캐시를 정리한다. 새 버전 안내 후 사용자가 새로고침하게 하고 작성·업로드 중 자동 새로고침하지 않는다.

### 인터페이스

세로 화면을 우선하되 가로 모드를 강제로 제한하지 않는다. 기기 safe area, 가상 키보드, 터치 영역을 고려한다. 본문과 날짜·거리 숫자를 읽기 쉬운 크기로 제공하고 포커스·라벨·오류 메시지·모달 닫기 등 접근성을 갖춘다. 로딩·빈 데이터·권한 없음·네트워크 실패 상태를 각각 설계한다.

## 12. 배포 및 예약 작업

개발·미리보기는 운영과 분리된 Supabase 프로젝트를 사용한다. 미리보기 주소가 운영 DB를 수정하지 않도록 환경 변수 범위를 구분한다. 운영 도메인과 Supabase Auth의 사이트 URL·리디렉션 허용 목록을 설정한다.

환경 변수 예시:

- NEXT_PUBLIC_SUPABASE_URL
- NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY
- SUPABASE_SERVICE_ROLE_KEY 또는 선택한 SDK에 맞는 서버 전용 비밀 키
- CRON_SECRET
- NEXT_PUBLIC_SITE_URL

키 이름과 SDK 방식은 개발 시 공식 문서에 맞춰 확정하고 `.env.example`에는 값 없는 예시만 둔다.

예약 작업은 주간 경계를 검사하고 누락된 시작·마감을 복구하는 조정 작업으로 구성한다. 목표 실행 간격은 5분이며 필요한 Vercel 요금제와 기능 지원을 개발 시작 시 확인한다. 새 주차의 화면 전환과 승인 마감 판정은 예약 실행의 실제 지연과 무관하게 서버 시각으로 적용한다. 예약 처리와 알림 생성은 통상 다음 실행에서 완료되게 한다.

엔드포인트는 CRON_SECRET을 검증하고, 여러 실행이 겹쳐도 주차별 잠금과 유일 제약으로 중복 처리를 막는다. 승인 마지막 건 처리 시에는 조기 확정 로직을 같은 트랜잭션으로 호출한다.

현재 배포 플랜이 목표 간격을 지원하지 않으면 개발자가 임의로 주간 정책을 변경하거나 유료 결제를 진행하지 않는다. 지원 여부와 비용 선택을 배포 전 보고하고, 동일한 마감 함수를 다른 예약 수단에서 호출할 수 있도록 분리해 둔다.

마이그레이션 적용 절차, 배포 방법, 도메인 연결, 이메일 인증·비밀번호 재설정 설정, 예약 작업 검증, 장애 시 수동 재실행 절차를 README에 작성한다. 집계 작업의 시작·완료·실패와 영향받은 주차 ID를 로그로 남기며 비밀 키·서명 URL·개인 사진 내용은 로그에 남기지 않는다.

## 13. 구현 순서와 산출물

### 단계 1 — 기반

Next.js 프로젝트, TypeScript, UI 기초, Supabase 개발 환경, 마이그레이션, RLS, 인증·프로필 완성 흐름을 구축한다.

### 단계 2 — 그룹

생성, 초대, 요청, 승인·거절·취소, 1인 1그룹 제약, 관리자 위임·탈퇴·보관을 구현한다.

### 단계 3 — 기록

사진 업로드, 중복 제출 방지, 기록 상세, 상태 전이, 관리자 검토, 댓글을 구현한다.

### 단계 4 — 대시보드

주차 탐색, 달력, 나의 진행률, 공동 순위, 준비 주간 표시, 과거 기록을 구현한다.

### 단계 5 — 주간 집계

평가 대상 스냅샷, 변경 예약, 마감 유예·기한 만료, 확정 결과, 알림, 한 번 표시, 이미지 저장을 구현한다.

### 단계 6 — PWA 및 배포 검증

아이콘·매니페스트·서비스 워커, 설치 안내, 오프라인 화면, 실제 모바일 검증, Vercel 미리보기 배포, 운영 배포 절차를 완성한다.

필수 산출물은 소스 코드, DB 마이그레이션·RLS, 시드 데이터, 환경 변수 예시, 테스트, README, 배포 설정, 검증 결과다. 최종 보고에는 구현 완료 항목과 실제 검증하지 못한 항목을 구분한다.

## 14. 인수 테스트

자동 테스트는 집계·권한·동시성 등 오류 영향이 큰 규칙에 집중하고, 설치·카메라·모바일 배치는 실제 환경에서 검증한다.

| 영역 | 합격 기준 |
|---|---|
| 가입 | 인증·프로필 완료 전 그룹 기능 접근이 차단되고 완료 후 진입 가능 |
| 초대 | 미로그인 초대 진입 후 가입해도 원래 그룹 요청으로 연결 |
| 중복 참여 | 동시 승인·그룹 생성 요청에도 활성 그룹은 하나 |
| 관리자 | 위임 시 활성 관리자는 한 명, 일반 회원의 검토 요청 거절 |
| 사진 | 여러 사진 제출·부분 실패 재시도·HEIC 입력 처리·권한 검증 정상 |
| 중복 등록 | 동일 제출 재시도에도 기록 한 건만 생성 |
| 상태 | pending/rejected/expired 제외, approved만 합산 |
| 수정 경합 | 관리자 검토 중 변경된 기록을 오래된 내용으로 승인하지 않음 |
| 공동 순위 | 동점에서 1, 2, 2, 4, 준비 주간은 순위 제외 |
| 주중 가입 | 준비 주간에는 실패·벌칙 제외, 다음 주 평가 포함 |
| 탈퇴 | 주초 평가 대상은 탈퇴 후에도 그 주 결과에 남고 데이터 접근은 차단 |
| 주간 경계 | 일요일 23:59와 월요일 00:00이 서로 다른 주차에 속함 |
| 승인 유예 | 월요일 오전 승인된 일요일 기록은 지난주에 반영 |
| 마감 | 12:00 이후 승인 거절, 미처리 기록 expired, 결과 중복 확정 없음 |
| 조기 확정 | 마지막 pending 처리 후 종료 주차 즉시 확정, 이후 변경 차단 |
| 과거 보존 | 목표·벌칙·그룹명·닉네임 변경에도 확정 스냅샷 유지 |
| 0km | 기록 없는 평가 대상자도 실패 결과에 포함 |
| 자동 요약 | 잠정 표시 후 확정 때 재팝업 없음, 기기 간 claim 중복 없음 |
| 이미지 | 전체 회원이 페이지에 포함되고 잠정 표시·한글·프로필 렌더링 정상 |
| 권한 | 타 그룹 기록·댓글·사진 및 임의 승인 API 접근 차단 |
| 예약 복구 | 지연·중복 실행 시 누락 주차 복구, 결과·알림 중복 없음 |
| PWA | iOS Safari·Android Chrome 설치 흐름 및 standalone 실행 검증 |
| 오프라인 | 개인화 응답 미캐시, 안내 화면 표시, 미전송 성공 표시 없음 |
| 업데이트 | 작성 중 강제 갱신 없이 새 버전 적용 가능 |
| 배포 | 타입 검사·린트·프로덕션 빌드·핵심 테스트 통과, 운영 환경 분리 확인 |

## 15. Claude Code 작업 지시

이 기획서를 기준으로 초기 버전을 구현하라. 먼저 저장소의 기존 구조와 지침을 확인하고, 기술 버전 및 Vercel·Supabase의 실제 지원 조건을 공식 문서로 검증하라. 기존 코드가 있으면 재사용하고 불필요하게 재구성하지 마라.

제품 정책은 이 문서의 확정값을 사용한다. 구현을 막지 않는 UI 세부사항은 모바일 사용성과 일관성을 기준으로 결정한다. DB 무결성·RLS·상태 전이·주간 마감을 먼저 보장하고 화면을 연결하라. 데모 데이터만으로 완료 처리하지 말고 실제 Supabase 연결을 통한 주요 흐름을 검증하라.

실제 계정·환경 변수·운영 도메인 또는 유료 서비스 선택이 필요한 단계는 필요한 값을 구체적으로 정리하라. 준비 가능한 코드·마이그레이션·문서·미리보기 검증을 먼저 마친 뒤 운영 배포에 필요한 마지막 조치를 안내하라. 완료 보고에는 검증 증거와 미완료 항목을 명확히 기재하라.
